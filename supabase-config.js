window.SEVENK_SUPABASE_CONFIG = {
  url: "",
  publishableKey: "",
  // Supabase Auth redirects should use http(s), for example http://localhost:4173 or your live domain.
  redirectTo: `${window.location.origin}${window.location.pathname}`,
};
